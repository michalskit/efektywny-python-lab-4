# Napisz poniższą funkcję korzystając (jeśli jest taka potrzeba)
# z funkcji: reduce, map, filter, lambda

# Funkcja reduce przyjmuje dwa argumenty: funkcję dwuargumentową i sekwencję.
# Aplikuje funkcję dwuargumentową kumulatywnie do elementów sekwencji,
# od lewej do prawej, tak by zredukować sekwencję do pojedynczej wartości.
# Na przykład: reduce(lambda x, y: x+y, [1, 2, 3, 4]) zwróci 10,
# ponieważ 1 + 2 + 3 + 4 = 10.

def reverse_nonpalindromes(words):
    pass